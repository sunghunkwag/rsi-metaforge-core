# ASCENT RESULT — Phases K–O, Directive-Level Scoring

The directive's success condition is not a claim: it is a running flywheel
whose every turn is certified. This document states, per the
pre-registered criteria of the five phase registers, exactly which
frontiers crossed and which did not. Misses and stalls are in the table,
not in a footnote. Every number below is gate-certified, measured on
frozen instruments, and reproduced byte-identically twice; the per-phase
RESULT documents carry the full records.

## The flywheel, turn by turn

| Phase | Module | Headline (certified) | Stalls and negatives (certified too) |
| --- | --- | --- | --- |
| K | M1 witness-sealed setter–solver | 1 admitted task family crossed: frontier-failure certificate at B_eval = 1500 at admission, solved at the same budget one generation later with own-solution macros excluded; lineage credited; pose allocation followed the credit (`docs/K_RESULT.md`) | the flywheel turned exactly once at these budgets; 9 frontier markers; Track A never crossed; the reward paid the crossing lineage, not the macro-supplying one |
| L | M2 MDL master gate | one frozen coding formula (K5) judged all vocabulary growth: 2 adoptions (both compress the sealed corpus and generalize to the frozen holdout), 203 rejections with exact bit deltas; the gated library crossed 3 frontiers vs K's 1 at identical budgets, including a certified 4-token route shorter than the task's own witness (`docs/L_RESULT.md`) | the six raw K bodies scored zero certified compression value in the retrofit; strict holdout deltas were measured vacuous and the savings clause was registered instead, before freeze |
| M | M5 ledger-trained amortizer | passes-per-budget on 48 sealed holdout families: 2 → 4 (v0) → 6 (ablation retrain), adopted; the neural retrain scored 3 and was rolled back on record (`docs/M_RESULT.md`) | the attribution is shallow op-transition statistics (the ablation proves it); the training ledger is thin by honest construction |
| N | M3 proof kernel & formal domain | kernel audited and frozen (K6); 35 theorems adopted, all kernel-checked, proofs in the ledger; 8 refutations with countermodels; 3 abbreviations through the master gate; ZERO axiom adoptions with the negative test constructing the attempts (`docs/N_RESULT.md`) | zero crossings, pre-registered as the expected outcome at b_eval = 20; the pool-restricted prover is the binding constraint; the domain frontier is derivability-under-budget, per the directive's own scope note |
| O | M4 meta-improvement & recursion closure | both channels live with adoptions AND rejections: Channel B adopted 2 kernel-certified search transformations and refused a false premise; Channel A adopted the amortizer and the MOD grant (extender protocol end-to-end: impossibility 0/6 → crossing 2/6) and rolled back the useless grant and the worse setter policy; union-battery passes 2 → 8 at equal budgets (`docs/O_RESULT.md`) | the propositional skeletons certify the logic while executable checks certify the premises — the registered v0 split; candidate order is frozen, not claimed optimal |

## Directive definition-of-done, scored

- **All five phases implemented, spec-frozen, predicted, run, and
  scored** — specs `ASCENT_{K,L,M,N,O}_SPEC.md`, registers
  `PREDICTIONS_{K,L,M,N,O}.md`, records `{K,L,M,N,O}_RESULT.md`.
  Prediction score across the ascent line: 44 confirmed, 3 misses. The
  three misses are the literal suite-count clauses of the L/M/N registers
  (231/243/258): the registers assumed per-phase code commits, but the
  single-file architecture led to one L–O freeze commit, so the suite at
  every finals run reports 270. Scored as misses, not reinterpreted; the
  substance of each clause (per-phase guards green, zero failures, prior
  pins verified) holds.
- **Every registered arm byte-identical across two independent runs** —
  asserted locally for every battery and by the Full Evidence workflow
  (each ascent battery runs twice per CI execution with digest equality
  and artifact `cmp` asserted).
- **`docs/05_limitations.md` in Target-Property form** — every
  open-endedness disclaimer converted to Target Property +
  Pre-Registered Criterion; the claim-boundary language kept and
  sharpened, nothing silently deleted.
- **Full Evidence green at the final commit** — the workflow runs on the
  default branch; the merge commit's run is the binding record.
- **Constitution intact** — the frozen kernel (K1 vault, K2 instruments,
  K3 ledger+replay, K4 meter, K5 formula, K6 proof kernel, K7 anti-cheat
  battery) is hash-pinned per phase and verified at every battery start;
  no gate was weakened, parametrized, or bypassed from mutable code at
  any point in the record; zero axiom self-adoptions; no test deletions;
  CI extended additively only.

## What did NOT happen (stated per §0 of the directive)

No claim of AGI, singularity, unbounded capability, or intelligence
explosion is made anywhere in this record, because none is supported: the
system's claims are exactly its gate-certified results. The flywheel
turned a small, counted number of times, each turn certified, and the
records state precisely where and why it stalled. The pre-registered
criteria under which stronger partial claims could ever be made are in
`docs/05_limitations.md`; none has been met.
